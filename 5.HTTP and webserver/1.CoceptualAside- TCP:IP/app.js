/*
client -server model(browser /webserver model) : How they identify to each other , where TCP/IP comes into the picture. every machine has IP address. Based on IP address client-server identify to each other (It creates socket -line where information transfer and it send the information via TCP (information has own type of structure( which uses HTTP/FTP/SMTP)))

Socket : connection between to 2 computer ...
*/